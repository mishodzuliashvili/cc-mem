# Python
__pycache__/
*.pyc
.venv/

# cc-mem runtime
*.db
*.db-wal
*.db-shm

# NOTE: .cc-mem/ (this repo's project memory) is intentionally COMMITTED — it's
# the team-shared, merge-friendly memory. Do not ignore it.

# OS
.DS_Store
